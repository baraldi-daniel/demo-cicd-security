bundle:
  name: sales-etl-demo

include:
  - resources/*.yml

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-3250511655996160.0.azuredatabricks.net  # <--- Coloque sua URL aqui

  prod:
    mode: production
    workspace:
      host: https://adb-3250511655996160.0.azuredatabricks.net  # <--- Coloque sua URL aqui
      root_path: /Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}
    run_as:
      user_name: daniel.baraldi@databricks.com
