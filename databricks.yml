bundle:
  name: sales-etl-demo

include:
  - resources/*.yml

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-7405614218138716.16.azuredatabricks.net/ 

  prod:
    mode: production
    workspace:
      host: https://adb-7405614218138716.16.azuredatabricks.net  
      root_path: /Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}
    run_as:
      user_name: daniel.baraldi@databricks.com
