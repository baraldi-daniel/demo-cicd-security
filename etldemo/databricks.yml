# This is a Databricks asset bundle definition for etldemo.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: etldemo
  uuid: 38c9f1a9-3c99-481d-99c8-7aa89b433bb4

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-3250511655996160.0.azuredatabricks.net

  prod:
    mode: production
    workspace:
      host: https://adb-3250511655996160.0.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/daniel.baraldi@databricks.com to make sure we only have a single copy.
      root_path: /Workspace/Users/daniel.baraldi@databricks.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: daniel.baraldi@databricks.com
        level: CAN_MANAGE
